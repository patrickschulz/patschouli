\RequirePackage{amsmath}

\RequirePackage{textcomp}
\RequirePackage{fontspec}
\RequirePackage{unicode-math}
\RequirePackage{microtype}

\DeclareOption{libertine}{
    \setmainfont{LibertinusSerif}[
        UprightFont    = *-Regular,
        BoldFont       = *-Bold,
        ItalicFont     = *-Italic,
        BoldItalicFont = *-Italic,
        Extension      = .otf,
        Path           = /home/patrick/texmf/tex/latex/patschouli/fonts/
    ]
    \setsansfont{LibertinusSans}[
        UprightFont    = *-Regular,
        BoldFont       = *-Bold,
        ItalicFont     = *-Italic,
        Extension      = .otf,
        Path           = /home/patrick/texmf/tex/latex/patschouli/fonts/
    ]
    \setmathfont{LibertinusMath-Regular}[
        Extension = .otf,
        Path=/home/patrick/texmf/tex/latex/patschouli/fonts/
    ]
}
\DeclareOption{computermodern}{
    \relax % nothing to do, since this is the default
}
\ExecuteOptions{computermodern}

\ProcessOptions\relax

\RequirePackage{tikz}
\RequirePackage{pgfplots}

\RequirePackage[per-mode=fraction, exponent-product=\cdot]{siunitx}
\DeclareSIUnit{\bit}{bit}
\DeclareSIUnit{\baud}{Baud}
\DeclareSIUnit{\dBc}{dBc}
\DeclareSIUnit{\dBcHz}{dBc/Hz}

\input{tikzsettings}
\input{pgfplotssettings}
\input{circuitsymbols}

\frenchspacing

\endinput
